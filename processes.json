{
    "apps" : [{
        "name"        : "dashboard",
        "script"      : "./app/launcher.js",
        "error_file" : "/var/log/node/dashboard-err.log",
        "out_file"   : "/var/log/node/dashboard-out.log",
        "pid_file"   : "./node.pid",
        "exec_mode"  : "fork_mode",
        "env": {
            "NODE_ENV": "production"
        }
    }]
}
